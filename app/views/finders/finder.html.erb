<%= render 'layouts/finder' %>
<div class= "container">
    <div class= "row mt-">
        <div class= "col-md-3">
            <%= render 'layouts/userinfo', user: current_user %>
            <%= render 'travels/newform', travel: @travel %>
        </div>
        <% if @range == "User" %>
            <div class= "col-md-8 offset-md-1">
                <h2 class= "mb-3">Travels</h2>
                <table class= "table table-hover">
                    <thead>
                        <tr>
                            <th class= "w-25"></th>
                            <th>Name</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @users.each do |user| %>
                        <tr>
                            <td align= "end" class= "pr-5">
                                <%= link_to user_path(user) do %>
                                    <%= attachment_image_tag user, :profile_image, size: '50x50', fallback: "no-img.jpg", class: "img" %><br>
                                <% end %>
                            </td>
                            <td><%= user.name %></td>
                            <td><%= link_to "詳細", user_path(user), class: "btn btn-success btn btn-sm" %></td>
                        </tr>
                    <% end %>
                    </tbody>
                </table>
            </div>
        <% else %>
            <div class= "col-md-8 offset-md-1">
                <h2 class= "mb-3">Travels</h2>
                <table class= "table table-hover">
                    <thead>
                        <tr>
                            <th class="w-25"></th>
                            <th>Title</th>
                            <th>Opinion</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    <% @travels.each do |travel| %>
                        <tr>
                            <td align= "end" class= "pr-5">
                                <%= link_to user_path(travel.user) do %>
                                    <%= attachment_image_tag travel.user, :profile_image, size: '50x50', fallback: "no-img.jpg", class: "img" %><br>
                                <% end %>
                            </td>
                            <td><%= travel.title %></td>
                            <td><%= travel.body %></td>
                            <td><%= link_to "詳細", travel_path(travel), class: "btn btn-success btn btn-sm" %></td>
                        </tr>
                    <% end %>
                    </tbody>
                </table>
            </div>
        <% end %>
    </div>
</div>
